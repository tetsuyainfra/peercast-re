[package]
name = "peercast-gnuid"
version = "0.1.0"
edition = "2024"

[features]
default = ["serde"]

serde = ["dep:serde"]

[dependencies]
uuid = { version = "1.23.0", features = ["v4"] }

serde = { version = "1", features = ["derive"], optional = true}

[dev-dependencies]
serde_json = "1"
